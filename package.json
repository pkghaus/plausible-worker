{
  "name": "plausible-worker",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "msw": "^2.15.0",
    "vitest": "^5.0.0",
    "wrangler": "4.129.0"
  }
}
